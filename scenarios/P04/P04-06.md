# P04-06: BaseModel ミックスイン

> **シナリオ**: 自分で気付く「モデルのid/created_at/updated_atが重複」
> **想定時間**: 5h
> **スキルタグ**: P6（リファクタリング）, P2（DB設計）

---

## Learn（概念理解）

### ミックスイン（Mixin）とは

**複数のクラスで共通する機能を再利用する**パターン:

```python
# ❌ Bad: 全モデルで id, created_at を繰り返し定義
class User(Base):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True)
    created_at = Column(DateTime, server_default=func.now())
    name = Column(String(255))

class Project(Base):
    __tablename__ = "projects"
    id = Column(Integer, primary_key=True)
    created_at = Column(DateTime, server_default=func.now())
    name = Column(String(255))

class Task(Base):
    __tablename__ = "tasks"
    id = Column(Integer, primary_key=True)
    created_at = Column(DateTime, server_default=func.now())
    title = Column(String(255))

# ✅ Good: ミックスインで共通化
class TimestampMixin:
    """タイムスタンプフィールドのミックスイン"""
    id = Column(Integer, primary_key=True, index=True)
    created_at = Column(DateTime, nullable=False, server_default=func.now())
    updated_at = Column(DateTime, nullable=False, server_default=func.now(), onupdate=func.now())

class User(Base, TimestampMixin):
    __tablename__ = "users"
    name = Column(String(255))

class Project(Base, TimestampMixin):
    __tablename__ = "projects"
    name = Column(String(255))

class Task(Base, TimestampMixin):
    __tablename__ = "tasks"
    title = Column(String(255))
```

### SQLAlchemy のミックスインパターン

```python
from sqlalchemy import Column, Integer, DateTime, String
from sqlalchemy.sql import func
from sqlalchemy.ext.declarative import declarative_base

Base = declarative_base()

class TimestampMixin:
    """全モデル共通のタイムスタンプ"""
    created_at = Column(DateTime, nullable=False, server_default=func.now())
    updated_at = Column(DateTime, nullable=False, server_default=func.now(), onupdate=func.now())

class PrimaryKeyMixin:
    """全モデル共通の主キー"""
    id = Column(Integer, primary_key=True, index=True)

# 複数ミックスインの組み合わせ
class BaseModel(Base, PrimaryKeyMixin, TimestampMixin):
    """全モデルの基底クラス"""
    __abstract__ = True  # このクラス自体はテーブルを作らない

class User(BaseModel):
    __tablename__ = "users"
    name = Column(String(255), nullable=False)
    # id, created_at, updated_at は自動的に継承される
```

### updated_at の自動更新

```python
# onupdate で更新時に自動的にタイムスタンプを更新
updated_at = Column(DateTime, nullable=False, server_default=func.now(), onupdate=func.now())

# テスト
user = User(name="Alice")
db.add(user)
db.commit()
print(user.created_at)  # 2024-01-01 10:00:00

# 更新
user.name = "Alicia"
db.commit()
print(user.updated_at)  # 2024-01-01 10:05:00（自動更新）
```

### マイグレーションでのカラム追加

既存のモデルに updated_at を追加する場合:

```python
# alembic/versions/xxxx_add_updated_at.py
def upgrade():
    # User
    op.add_column('users', sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))

    # Project
    op.add_column('projects', sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))

    # Task
    op.add_column('tasks', sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))

def downgrade():
    op.drop_column('users', 'updated_at')
    op.drop_column('projects', 'updated_at')
    op.drop_column('tasks', 'updated_at')
```

---

## Practice（練習課題）

### 課題: 以下のモデルを BaseModel ミックスインで共通化せよ

```python
class Product(Base):
    __tablename__ = "products"
    id = Column(Integer, primary_key=True)
    created_at = Column(DateTime, server_default=func.now())
    name = Column(String(255))
    price = Column(Float)

class Category(Base):
    __tablename__ = "categories"
    id = Column(Integer, primary_key=True)
    created_at = Column(DateTime, server_default=func.now())
    name = Column(String(255))

class Order(Base):
    __tablename__ = "orders"
    id = Column(Integer, primary_key=True)
    created_at = Column(DateTime, server_default=func.now())
    user_id = Column(Integer, ForeignKey("users.id"))
    total = Column(Float)
```

**Q1.** BaseModel ミックスインを作成せよ（id, created_at, updated_at）。
**Q2.** 各モデルをリファクタリングせよ。
**Q3.** マイグレーションで updated_at を追加するコードを書け。

### 模範解答

<details>
<summary>クリックで表示</summary>

**A1. BaseModel ミックスイン**:

```python
class BaseModel(Base):
    __abstract__ = True  # テーブルを作らない

    id = Column(Integer, primary_key=True, index=True)
    created_at = Column(DateTime, nullable=False, server_default=func.now())
    updated_at = Column(DateTime, nullable=False, server_default=func.now(), onupdate=func.now())
```

**A2. リファクタリング後**:

```python
class Product(BaseModel):
    __tablename__ = "products"
    name = Column(String(255), nullable=False)
    price = Column(Float, nullable=False)

class Category(BaseModel):
    __tablename__ = "categories"
    name = Column(String(255), nullable=False)

class Order(BaseModel):
    __tablename__ = "orders"
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    total = Column(Float, nullable=False)
```

**A3. マイグレーション**:

```python
def upgrade():
    op.add_column('products', sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))
    op.add_column('categories', sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))
    op.add_column('orders', sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))

def downgrade():
    op.drop_column('products', 'updated_at')
    op.drop_column('categories', 'updated_at')
    op.drop_column('orders', 'updated_at')
```

</details>

---

## Mission（本番課題）

### 状況

コードを見直していると、User, Project, Task の全モデルで `id` と `created_at` が重複していることに気づきました。さらに、`updated_at` がないため、いつ更新されたかわかりません。

TL に相談すると:

> 「BaseModel ミックスインを作って、全モデルで共通化しよう。ついでに updated_at も追加して。」

### やること

1. **GitHub Issue を作成する**
   - タイトル: `refactor: BaseModel ミックスインと updated_at を追加`
   - 本文: 重複フィールド、updated_at の必要性

   <details>
   <summary>Issue の書き方例</summary>

   ```markdown
   ## 概要
   全モデルに共通する `id`, `created_at`, `updated_at` フィールドを
   BaseModel ミックスインに集約し、重複を解消する。

   ## 現状の問題
   - `id`, `created_at` が全モデル（User, Project, Task）に重複定義されている
   - `updated_at` が一部のモデルにない（変更日時が追えない）

   ## 解決策
   ```python
   class TimestampMixin:
       id: int = Column(Integer, primary_key=True, index=True)
       created_at: datetime = Column(...)
       updated_at: datetime = Column(..., onupdate=func.now())
   ```

   ## 受入条件
   - [ ] `models/base.py` に `TimestampMixin` を作成
   - [ ] User, Project, Task, TaskActivity が `TimestampMixin` を継承
   - [ ] `updated_at` が全モデルに追加されている
   - [ ] Alembic マイグレーションが作成・適用されている
   - [ ] 全テストが通る
   ```

   **成長ポイント**:
   - **コード例を Issue に添付**: 解決策のイメージを共有する
   - **受入条件にマイグレーションを含める**: DB変更を忘れない
   </details>

2. **ブランチを作成する**
   ```bash
   git checkout -b refactor/proj04-base-model-mixin
   ```

3. **BaseModel ミックスインを作成する**
   - `models.py` に追加:
     - `__abstract__ = True`（テーブルを作らない）
     - `id`: primary key, index
     - `created_at`: server_default=func.now()
     - `updated_at`: server_default=func.now(), onupdate=func.now()

4. **全モデルをリファクタリングする**
   - User, Project, Task が BaseModel を継承
   - 重複する id, created_at を削除

5. **Alembic マイグレーションを作成する**
   - updated_at カラムを全テーブルに追加
   - server_default=func.now() を設定

6. **マイグレーションを適用する**
   ```bash
   alembic revision --autogenerate -m "add updated_at to all tables"
   alembic upgrade head
   ```

7. **Pydantic スキーマを更新する**
   - UserResponse, ProjectResponse, TaskResponse に updated_at を追加

8. **テストを更新する**
   - updated_at が返されることを確認

9. **全テストが通ることを確認する**
   ```bash
   pytest tests/ -v
   ```

10. **PR を作成する**

   <details>
   <summary>PR の書き方例</summary>

   ```markdown
   ## 概要
   TimestampMixin を導入し、全モデルの共通フィールドを集約しました。
   あわせて全モデルに `updated_at` を追加しました。

   Closes #XX

   ## 変更内容
   - `models/base.py`: `TimestampMixin` 新設
   - `models/*.py`: User, Project, Task, TaskActivity が Mixin を継承
   - `migrations/xxxx_add_updated_at.py`: `updated_at` カラム追加

   ## Before / After（重複削除量）
   - `id` カラム定義: 4箇所 → 1箇所（Mixin）
   - `created_at` 定義: 4箇所 → 1箇所
   - 追加: `updated_at` が全モデルで自動更新される

   ## 注意点
   - `updated_at` は DB の `ON UPDATE CURRENT_TIMESTAMP` で自動設定
   - 既存データの `updated_at` は NULL → マイグレーションで `created_at` と同値にした

   ## レビュー観点
   - [ ] Mixin の継承順序が正しいか（MRO の問題がないか）
   - [ ] `updated_at` が PUT リクエスト時に自動更新されるか
   - [ ] 既存データの `updated_at` が適切な値になっているか
   ```

   **成長ポイント**:
   - **「注意点」セクション**: 落とし穴・既存データへの影響を明示
   - **マイグレーションの扱いを説明**: DBの変更は詳細に記録する
   </details>

### Git 操作の参考

```bash
git checkout -b refactor/proj04-base-model-mixin

git add models.py schemas.py alembic/versions/*.py tests/

git commit -m "refactor: introduce BaseModel mixin with updated_at

- Create BaseModel mixin (id, created_at, updated_at)
- Refactor User, Project, Task to inherit BaseModel
- Remove duplicated id and created_at fields
- Add updated_at column to all tables via migration
- Update Pydantic schemas to include updated_at
- Update tests for new field

Closes #XX"

git push -u origin refactor/proj04-base-model-mixin
```

### ヒント

<details>
<summary>BaseModel の実装例</summary>

```python
# models.py
from sqlalchemy import Column, Integer, DateTime
from sqlalchemy.sql import func
from sqlalchemy.ext.declarative import declarative_base

Base = declarative_base()

class BaseModel(Base):
    """全モデルの基底クラス"""
    __abstract__ = True  # このクラス自体はテーブルを作らない

    id = Column(Integer, primary_key=True, index=True)
    created_at = Column(DateTime, nullable=False, server_default=func.now())
    updated_at = Column(DateTime, nullable=False, server_default=func.now(), onupdate=func.now())

class User(BaseModel):
    __tablename__ = "users"
    name = Column(String(255), nullable=False, unique=False)
    email = Column(String(255), nullable=False, unique=True)

class Project(BaseModel):
    __tablename__ = "projects"
    name = Column(String(255), nullable=False, unique=True)
    description = Column(Text, nullable=True)

class Task(BaseModel):
    __tablename__ = "tasks"
    project_id = Column(Integer, ForeignKey("projects.id"), nullable=False)
    title = Column(String(255), nullable=False)
    # ...
```

</details>

<details>
<summary>マイグレーションの例</summary>

```python
# alembic/versions/xxxx_add_updated_at.py
def upgrade():
    op.add_column('users', sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))
    op.add_column('projects', sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))
    op.add_column('tasks', sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))
    op.add_column('task_activities', sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))

def downgrade():
    op.drop_column('users', 'updated_at')
    op.drop_column('projects', 'updated_at')
    op.drop_column('tasks', 'updated_at')
    op.drop_column('task_activities', 'updated_at')
```

</details>

---

## Review（自己採点チェックリスト）

### 必須項目

- [ ] BaseModel ミックスインが作成されている
- [ ] BaseModel に id, created_at, updated_at がある
- [ ] `__abstract__ = True` が設定されている
- [ ] 全モデルが BaseModel を継承している
- [ ] 重複する id, created_at が削除されている
- [ ] updated_at カラムのマイグレーションが作成・適用されている
- [ ] Pydantic スキーマに updated_at が追加されている
- [ ] 全テストが通る

### 加点項目

- [ ] onupdate=func.now() が設定されている（自動更新）
- [ ] マイグレーションで server_default を設定している
- [ ] テストで updated_at の自動更新を確認している

### よくある間違い

- `__abstract__ = True` を忘れる（base_model テーブルが作られる）
- onupdate を設定しない（手動で updated_at を更新する必要がある）
- 既存データの updated_at が NULL になる（server_default がない）
- Pydantic スキーマを更新しない（レスポンスに updated_at が含まれない）
- マイグレーションを適用しない（DB にカラムがない）
