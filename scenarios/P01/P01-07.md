# P01-07: ヘルスチェックに DB 接続確認を追加

> **シナリオ**: TLから「ヘルスチェックにDB接続確認も入れて」
> **想定時間**: 4h
> **スキルタグ**: P1（API設計）, P3（テスト）, P4（コード品質）

---

## Learn（概念理解）

### ヘルスチェックとは

`/health` エンドポイントは **「このサービスは正常に動いているか」** を返す。
ロードバランサーや監視ツールが定期的に叩いて、異常があればアラートを出す。

### なぜ DB 確認が必要か

```
# ❌ 今の実装
GET /health → {"status": "ok"}

# これだと:
# - API プロセスは動いている ✅
# - でも DB が落ちている ❌ → ユーザーリクエストは全て500
# - なのに health は "ok" と返す 😱
```

**DB に接続できないのに "ok" を返すのは嘘** 。

### 良いヘルスチェックの設計

```python
@app.get("/health")
def health_check(db: Session = Depends(get_db)):
    try:
        db.execute(text("SELECT 1"))
        return {
            "status": "healthy",
            "database": "connected",
        }
    except Exception as e:
        return JSONResponse(
            status_code=503,
            content={
                "status": "unhealthy",
                "database": "disconnected",
            },
        )
```

**ポイント:**
- DB に `SELECT 1` を実行して接続確認
- 異常時は `503 Service Unavailable` を返す
- ステータスコードで異常を伝える（ロードバランサーはステータスコードを見る）

### ヘルスチェックのレベル

| レベル | 確認内容 | 例 |
|-------|---------|-----|
| Liveness | プロセスが生きているか | `{"status": "ok"}` |
| Readiness | リクエストを受け付けられるか | DB接続、依存サービスの確認 |
| Startup | 起動完了したか | 初期化処理の完了確認 |

実務では最低でも **Readiness** レベル（DB確認込み）が必要。

---

## Practice（練習課題）

### 課題: 以下のヘルスチェックの問題点を指摘し、改善せよ

```python
@app.get("/health")
def health():
    return {"status": "ok"}
```

このサービスは PostgreSQL と Redis に依存しているとする。

### 模範解答

<details>
<summary>クリックで表示</summary>

**問題点:**
1. DB（PostgreSQL）の接続確認をしていない
2. Redis の接続確認をしていない
3. 異常時でも 200 OK を返してしまう

**改善:**
```python
from fastapi.responses import JSONResponse
from sqlalchemy import text

@app.get("/health")
def health(db: Session = Depends(get_db)):
    health_status = {"status": "healthy"}
    is_healthy = True

    # DB確認
    try:
        db.execute(text("SELECT 1"))
        health_status["database"] = "connected"
    except Exception:
        health_status["database"] = "disconnected"
        is_healthy = False

    # Redis確認
    try:
        redis_client.ping()
        health_status["redis"] = "connected"
    except Exception:
        health_status["redis"] = "disconnected"
        is_healthy = False

    if not is_healthy:
        health_status["status"] = "unhealthy"
        return JSONResponse(status_code=503, content=health_status)

    return health_status
```

</details>

---

## Mission（本番課題）

### 状況

TL から Slack:

> 「ヘルスチェック、今 `{"status": "ok"}` だけ返してるよね。DB 落ちてても ok 返すのはまずい。DB の接続確認も入れてほしい。あと、異常時は 503 返すようにして。」

### やること

1. **現状の `/health` を確認する**
   - `curl http://localhost:8000/health` → 現状のレスポンスを確認

2. **GitHub Issue を作成する**
   - タイトル: `feat: ヘルスチェックに DB 接続確認を追加`

   <details>
   <summary>Issue の書き方例</summary>

   ```markdown
   ## 概要
   ヘルスチェックに DB 接続確認を追加し、真の稼働状態を返すようにする。

   ## 背景
   現状のヘルスチェックはプロセスが生きているかのみを確認しており、DB が落ちていても "ok" を返してしまう。

   ## タスク
   - [ ] /health エンドポイントに DB 接続確認を追加
   - [ ] 正常時: 200 + {"status": "healthy", "database": "connected"}
   - [ ] 異常時: 503 + {"status": "unhealthy", "database": "disconnected"}
   - [ ] テストを追加（正常系・異常系）

   ## 受入条件
   - [ ] DB 接続が成功する場合、200 を返す
   - [ ] DB 接続が失敗する場合、503 を返す
   - [ ] ヘルスチェックが軽量（SELECT 1）

   ## 参考資料
   - Kubernetes Readiness Probe
   - FastAPI ヘルスチェックベストプラクティス
   ```

   **ポイント**:
   - 現状の問題（DB落ちでもokを返す）を明確に
   - 正常時・異常時の挙動を明記
   </details>

3. **ブランチを作成する**
   - `feat/proj01-health-check-db`

4. **`/health` エンドポイントを改善する**
   - DB に `SELECT 1` を実行して接続確認
   - 正常時: `200` + `{"status": "healthy", "database": "connected"}`
   - 異常時: `503` + `{"status": "unhealthy", "database": "disconnected"}`

5. **テストを書く**
   - 正常時のテスト（DB接続成功）
   - DB が使えない場合のテスト（可能であれば）

6. **PR を作成する**

   <details>
   <summary>PR の書き方例</summary>

   ```markdown
   ## 概要
   ヘルスチェックに DB 接続確認を追加し、真の稼働状態を返すようにしました。

   Closes #XX

   ## 変更内容
   - /health エンドポイントに DB 接続確認を追加
   - DB 接続成功時: 200 + {"status": "healthy", "database": "connected"}
   - DB 接続失敗時: 503 + {"status": "unhealthy", "database": "disconnected"}
   - テストを追加

   ## 実装の詳細
   - SELECT 1 で DB 接続を確認（軽量クエリ）
   - 例外発生時は 503 Service Unavailable を返す
   - エラー詳細はログに出力し、レスポンスには含めない

   ## テスト
   - [ ] DB 接続成功時のテスト
   - [ ] 既存テストが全て通る

   ## レビュー観点
   - [ ] ヘルスチェックが軽量か
   - [ ] 異常時のステータスコードが適切か
   ```

   **ポイント**:
   - SELECT 1 で軽量なことを強調
   - 異常時の挙動を明確に
   </details>

### ヒント

<details>
<summary>SQLAlchemy で SELECT 1 を実行するには</summary>

```python
from sqlalchemy import text

db.execute(text("SELECT 1"))
```

`text()` でラップしないと SQLAlchemy 2.x ではエラーになります。

</details>

<details>
<summary>503 レスポンスを返すには</summary>

```python
from fastapi.responses import JSONResponse

return JSONResponse(
    status_code=503,
    content={"status": "unhealthy", "database": "disconnected"},
)
```

</details>

---

## Review（自己採点チェックリスト）

### 必須項目

- [ ] `/health` が DB 接続を確認している
- [ ] DB 接続成功時: `200` + `{"status": "healthy", "database": "connected"}`
- [ ] DB 接続失敗時: `503` + `{"status": "unhealthy", ...}`
- [ ] テストが追加されている
- [ ] GitHub Issue と PR が作成されている

### 加点項目

- [ ] `text("SELECT 1")` を使用している（SQLAlchemy 2.x 対応）
- [ ] DB 接続失敗時のテストがある
- [ ] レスポンスに DB 以外の情報（バージョンなど）も含めている
- [ ] 例外の詳細はログに出力し、レスポンスには含めない（情報漏洩防止）

### よくある間違い

- 例外発生時に `raise` してしまい 500 になる（503 を返すべき）
- 例外のエラーメッセージをそのままレスポンスに含める（セキュリティリスク）
- `SELECT 1` ではなく大きなクエリを実行してしまう（ヘルスチェックは軽量に）
- テストを書かない
